# api/.htaccess
<IfModule mod_rewrite.c>
RewriteEngine On
# Nada especial aquí, tu endpoint ya funciona como /api/rsvp.php?action=token
</IfModule>

# Seguridad básica
<FilesMatch "\.(ini|env|log|sql|sh)$">
  Require all denied
</FilesMatch>
